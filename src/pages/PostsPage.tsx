import PostsList from "../components/PostsList";

const PostsPage = () => {
  return (
    <div>
      <title>Latest Posts|Posty</title>
      <meta name="description" content="A list of all posts content on Posty" />
      <PostsList />
    </div>
  );
};

export default PostsPage;
